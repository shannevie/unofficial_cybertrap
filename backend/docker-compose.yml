version: '3.8'

services:
  domains_api:
    build: .
    command: ["/bin/domains_api"]
    ports:
      - "3000:3000"
    environment:
      - todo

  # artefacts_api:
  #   build: .
  #   command: ["/app/artefacts_api/main"]
  #   ports:
  #     - "3001:3000"

  # nuclei_scanner:
  #   build: .
  #   command: ["/app/nuclei_scanner/main"]
  #   ports:
  #     - "3002:3000"
